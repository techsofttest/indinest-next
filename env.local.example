# Public: used for image src and other browser-visible asset URLs
NEXT_PUBLIC_API_URL=http://indinest.test

# Server-only: used by SSR/RSC to reach the Laravel backend directly (never sent to browser)
BACKEND_URL=http://indinest.test